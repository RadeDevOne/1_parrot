/* eslint jsx-a11y/anchor-is-valid: 1 */
import type { FC } from "react";
import tw, { css, styled, theme } from "twin.macro";

const Carousel: FC = () => {
  return <section css={[tw`bg-gray-200`, tw`hover:text-indigo-600`]}></section>;
};

export default Carousel;
