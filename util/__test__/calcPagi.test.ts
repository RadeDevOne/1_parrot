import calcPagi from "../calcPagi";

describe("calculating pagination details", () => {
  it("should work properly", () => {
    const result = calcPagi(5, 16, 4, 70);

    // console.log({ result });
  });
});
